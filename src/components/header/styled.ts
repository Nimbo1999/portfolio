import styled from 'styled-components';

const HeaderWrapper = styled.header`

`;

const NavBar = styled.nav`

`;

const List = styled.ul``;

const ListItem = styled.li``;

export { HeaderWrapper, NavBar, List, ListItem };
